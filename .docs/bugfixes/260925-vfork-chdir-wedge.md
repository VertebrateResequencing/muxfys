- [x] A process that served a muxfys mount and started a child process whose working directory (exec.Cmd.Dir) was on that mount could deadlock forever. wr's runner does exactly this for a job with mounts and no explicit Mount, where the job's cwd is the mount root.
  - Go forks with CLONE_VFORK. The child chdirs before exec. Before this fix, that chdir sent a FUSE_ACCESS request: muxfys's Access (filesystem.go:935) returned fuse.OK, so the kernel kept asking on every access check. Only the parent could answer. If a GC stop-the-world started in that window, it could never preempt the vforking thread, so no goroutine served the request and the process wedged for good. See golang/go#11155, and go-fuse fs/api.go:152-176.
  - A realistic rate on Go 1.26.3, natural GC with 1 MB/s of allocation: about 1 wedge per 830 execs.
  - Red: standalone go-fuse harness scratchpad/fusefork-w1/ (w1_test.go, matrix.sh), with Access OK and a 1s TTL, cwd = mount root, and an exec loop under forced GC: `TRIALS=5 ./matrix.sh w1263.test go1.26.3 baseline=` gives `wedged=5/5`.
  - Proven workaround, same harness: Access returns ENOSYS, plus one access(2) on the mount root made by the serving process itself before any exec, so the kernel learns ENOSYS from a request that nothing else is waiting on. Result: `enosys-warm wedged=0/5` on both 1.25.5 and 1.26.3. The chdir then sends no FUSE request at all. There's no permission-semantics change, because Access already allowed everything.
  - Not covered: a cwd in a SUBDIRECTORY of the mount would also need long entry/attr timeouts. wr uses the mount root, so that's out of scope; document it.
  - Fixed. `MuxFys.Access` (filesystem.go) now returns `fuse.ENOSYS`, and `Mount()` (muxfys.go) calls `syscall.Access(mountPoint, X_OK)` itself straight after `WaitMount()`, so the kernel sets `no_access` from a request nothing else waits on. Every later access check, a chdir's included, is granted without a FUSE request. The `Mount()` doc comment and the README's Guidance section say a child's cwd must be the mount root, since a subdirectory would also need long entry/attr timeouts.
  - Regression test `TestMountForkChdir` (access_test.go) mounts real muxfys over the test `localAccessor`, in a re-exec'd copy of the test binary, then execs `/bin/true` with `cmd.Dir` on the mount root for 1s under back-to-back `runtime.GC()`. The parent treats 2s without output as a wedge. It then kills the child's process group, aborts only that mount's fusectl connection (the minor of its `0:N` st_dev in `/proc/self/mountinfo`, matched by mount point and `fuse.MuxFys` fstype, and rechecked before the abort), and runs `fusermount -u -z`. It pins 3 trials and skips if `/dev/fuse` or fusermount is unusable.
  - Red before the fix: `CGO_ENABLED=0 go test -tags netgo --count 1 . -run TestMountForkChdir` gave `wedged 3/3` on each of 4 runs (6.2s). With ENOSYS but without Mount's warm-up access it still wedged 2/3, so the warm-up is load-bearing. After the fix it passes 3/3 in 3.1s, on 5 plain runs and 2 `-race` runs. No mount, fusectl connection or process was left behind by any run.
  - `TestMountAccess` shows permissions are unchanged on a writable cached mount: `access(2)` with R|W|X succeeds on the root, a remote file and a created file, and `test -r`, `test -w` and `test -x` on the root and `test -w` on both files exit 0.
  - Gates: `make lint` 0 issues; `make test`, `make race` and `make coverage` ok (56.4%). `TestS3Localntegration` skips here, as it already did, because minio isn't installed.

- [x] CI fails before any test runs, on `develop` too: the "Install MinIO" step in `.github/workflows/tests.yml` downloads `https://dl.min.io/server/minio/release/linux-amd64/minio`, which now returns HTTP 410 Gone.
  - Red: `curl -sSI -o /dev/null -w '%{http_code}'` on that URL gives `410`. So do the `linux-amd64/archive/` index and a pinned archive binary under it. `dl.min.io/aistor/...` still answers 200, but that is the commercial AIStor build, not the community server.
  - `TestS3Localntegration` (s3_test.go) runs `minio server --address localhost:$MUXFYS_S3_PORT <dir>` itself, found with `exec.LookPath("minio")`. A Docker service container wouldn't fit that, because the test needs a `minio` binary on PATH that it can start and stop.
  - Fixed: the step now downloads the `minio.linux-amd64.RELEASE.2025-09-07T16-13-09Z` asset from the GitHub release of that tag, the latest release with binaries attached. It checks the file against the release's own SHA-256 (`7c5bd851...5fb855f`), then prints `minio --version`. The release and hash are step env vars.
  - Proof: running the step's exact `run` script from the workflow, with a scratch `HOME` and `GITHUB_PATH`, exits 0. It prints `minio: OK` and `minio version RELEASE.2025-09-07T16-13-09Z`, and it appends the bin dir to `GITHUB_PATH`. With that binary on PATH, `TestS3Localntegration` passes all its assertions against a real minio.
  - That run needed a non-NFS `HOME`. With `HOME` on this box's NFS home, its `~/` mount point case fails at s3_test.go:2110 with `fusermount exited with code 256`. The failure is identical on the base commit `8fafdc8`, so it comes from this machine, not from this branch. CI's home isn't on NFS.

- [x] PRRT_kwDOBWq4W86mGc0e, access_test.go:234: once a wedge was detected, the cleanup waited on `<-done` with no bound. If the killed child was never reaped, the test hung until `go test`'s 30m timeout instead of failing.
  - Fixed: the cleanup now waits at most `forkReapTimeout` (10s) for the child to exit. It runs `fusermount -u -z` either way, then asserts that the child was reaped, so a stuck child fails the trial quickly and still leaves no mount behind.
  - No red test: making it red needs a child that SIGKILL plus a fusectl abort can't reap, which can't be arranged. Instead, the wedge path was exercised with the fix removed (`filesystem.go` and `muxfys.go` from `8fafdc8`). All 3 trials wedged and every child was reaped within the bound. The only failing assertion was the wedge count, and no mount or fusectl connection was left afterwards.

- [x] PRRT_kwDOBWq4W86mGc1p, .docs/bugfixes/260925-vfork-chdir-wedge.md:2: the pre-fix behaviour was described in the present tense, as if Access still returned fuse.OK. The first item's summary and cause lines now use the past tense ("Access returned fuse.OK"). The rest of that item is dated measurements or already describes the fix. Docs only, so no test.

- [x] PRRT_kwDOBWq4W86mGc19, muxfys.go:156: `Mount()`'s warm-up access passed a hard-coded `accessExecute = 1` rather than a named X_OK. `syscall` has no X_OK on linux, but the module already depended on `golang.org/x/sys` (indirectly).
  - Fixed: the constant is gone, and `Mount()` now calls `unix.Access(fs.mountPoint, unix.X_OK)`. `TestMountAccess` also uses `unix.R_OK|unix.W_OK|unix.X_OK` in place of its own `accessRWX = 7`. `go mod tidy` moved `golang.org/x/sys v0.47.0` from the indirect to the direct require block, at the same version, so `go.sum` is unchanged.
  - No new test, because behaviour is unchanged: `TestMountForkChdir` and `TestMountAccess` still pass, and `GOOS=darwin go build .` still compiles.
  - Gates after all four items: `make lint` 0 issues, `make test` ok, `make race` ok. No mount or fusectl connection was left afterwards.

- [x] PRRT_kwDOBWq4W86mGjUZ, filesystem.go:937: the Access comment said the kernel treats ENOSYS as permission granted, without saying that only holds without the `default_permissions` mount option. With that option the kernel never sends FUSE_ACCESS. It checks mode bits from cached attributes itself, so checks are no longer always granted, and a chdir can again need a GETATTR once attributes expire.
  - Fixed, comments only: the Access doc comment now states the condition and what breaks without it. The `Mount()` doc comment says its "no more requests for a chdir" claim depends on it too. A comment on `mOpts` in `Mount()` warns against adding `default_permissions`, or `IDMappedMount`, which go-fuse v2.11.0 turns into `default_permissions` (fuse/mount_linux.go:96).
  - muxfys currently sets neither. `make lint` 0 issues, and `TestMountForkChdir` and `TestMountAccess` still pass.
