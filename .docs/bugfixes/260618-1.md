- [x] I've put my s3 creds in ~/.s3cfg and uncommented user_allow_other. You can set MUXFYS_S3_PORT to any free port. Try testing again, tell me if any tests still get skipped, and fix any failing tests.
  - Updated dependency, CI, and golangci-lint configuration for the current Go/tooling versions.
  - Fixed append handling for cached files and made FUSE/S3 timing-sensitive tests assert stable behavior instead of wall-clock timing.
  - Verified local MinIO-backed S3 tests with free ports; remote S3 still skips because `MUXFYS_REMOTES3_TARGET` is unset.
